{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
 "scripts": {
  "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) exit 0;; *) echo \"Please use pnpm\" && exit 1;; esac'",
  "build": "tsc",
  "start": "node dist/app.js",
  "dev": "ts-node src/app.ts",
  "typecheck:libs": "tsc --build",
  "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts/**\" --if-present run typecheck"
},
  "dependencies": {
  "express": "^4.18.2"
}
  "private": true,
  "devDependencies": {
  "prettier": "^3.8.3",
  "typescript": "~5.9.3",
  "@types/express": "^4.17.21",
  "@types/node": "^22.0.0",
  "ts-node": "^10.9.2"
}
}
